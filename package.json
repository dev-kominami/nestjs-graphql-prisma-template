{
  "name": "music-map",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "web/*",
      "web/backend/*"
    ],
    "nohoist": [
      "**/@nestjs",
      "**/@nestjs/**"
    ]
  },
  "scripts": {
    "api": "yarn workspace api",
    "api:start:dev": "yarn api start:dev",
    "api:test": "yarn api test",
    "prisma:generate": "yarn api prisma generate",
    "prisma:seed": "yarn api prisma:seed",
    "prisma:studio": "yarn api prisma:studio"
  }
}
